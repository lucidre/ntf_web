const Duration fastDuration = Duration(milliseconds: 300);
const Duration medDuration = Duration(milliseconds: 600);
const Duration slowDuration = Duration(milliseconds: 900);
const Duration xSlowDuration = Duration(milliseconds: 1200);
const Duration xxSlowDuration = Duration(milliseconds: 2000);
const Duration pageTransitionDuration = Duration(milliseconds: 200);
